# UTILIZANDO OBJETOS CON NODE

Crearemos un programa de Node que pueda modificar una variable con contenido en formato json, añadiendo objetos, y luego contar la cantidad de objetos que guarda el Array.